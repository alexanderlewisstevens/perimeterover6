# Local Paper Downloads

Downloaded PDFs should go in `papers/downloads/`. That directory is ignored by
git to avoid committing large files or publisher copies.

Use `literature/download_manifest.json` to track open-access PDFs that are
useful for source verification. When extracting results, copy only short
permitted snippets into notes and prefer paraphrase in the review.
